<h1>All Movies</h1>

<ul>
	<% Movie.all.each do |movie| %>
		<li>
			<h3><%= link_to movie.name, movie %></h3>
			<%= image_tag movie.avatar.url, class: "materialboxed", height: "300px" %>
			<p>Genre: <%= movie.genre %></p>
			<p>Location Filmed: <%= movie.location_filmed %></p>
			<p>Length: <%= distance_of_time_in_words(movie.length.seconds) %></p>
		</li>
	<% end %>	
</ul>